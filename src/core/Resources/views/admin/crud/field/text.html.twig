{%- block value -%}
    {%- if options.raw -%}
        {{- value|default(options.default_value)|raw -}}
    {%- else -%}
        {{- value|default(options.default_value)|trans -}}
    {%- endif -%}
{%- endblock -%}
